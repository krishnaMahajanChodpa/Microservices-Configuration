
eureka:
  client:
    service-url:
      defaultZone: ${DISCOVERY_URL:http://localhost:8761}/eureka/    
      
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    name: onlineservice
    password: root
    url: jdbc:mysql://localhost:3306/onlineservice
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    
